<view class="home_cate">
  <navigator wx:for="{{cateList}}" wx:key="name" open-type="navigate" url="{{item.navigator_url}}">
    <image mode="widthFix" src="{{item.image_src}}" />
  </navigator>
</view>